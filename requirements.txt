tweet-capture
# selenium might be a direct or indirect dependency, ensure it's installed if issues arise.
# You might need to specify versions if you encounter compatibility problems, e.g.:
# tweetcapture==0.1.5
Flask
gunicorn
Werkzeug
